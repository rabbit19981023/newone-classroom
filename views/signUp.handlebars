{{#if isSignUp}}
  {{#extend 'h1-class'}}
sign-up
  {{/extend}}
{{/if}}

{{#if isUser}}
  {{#extend 'title'}}
教室借用系統 - 使用者註冊
  {{/extend}}
{{/if}}

{{#if isUser}}
  {{#extend 'formPath'}}
/sign-up
  {{/extend}}
{{/if}}